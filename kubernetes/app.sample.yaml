#
# See this Helm chart for options
# https://github.com/benc-uk/helm-charts/tree/master/webapp
#

image:
  repository: bencuk/nodejs-demoapp
  tag: latest
  pullPolicy: Always

service:
  targetPort: 3000
  port: 3000
  type: LoadBalancer